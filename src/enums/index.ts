/**
 *  @module MarkerType 点位类型
 *  @description 枚举类型
 */
export enum markerType {
  /**
   * 动画类型点位
   */
  animation = "animation",
}

/**
 * 线路类型
 */
export enum lineType {
  /**
   * 轨迹线
   */
  track = "track",
}
